# frozen_string_literal: true

require 'rspec'

describe Aggregates::DomainMessage do
  context 'when generating default values' do
    it 'uses new aggregate ids each time' do
      # TODO
    end

    it 'uses new created_at timestamps each time' do
      # TODO
    end
  end

  context 'when handling JSON' do
    it 'it serializes and deserializes back to an equivalent object' do
      # TODO
    end
  end
end
