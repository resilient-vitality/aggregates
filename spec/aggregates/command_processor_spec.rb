# frozen_string_literal: true

require 'spec_helper'

describe Aggregates::CommandProcessor do
  context 'when processing a command' do
    it 'calls all handlers of that command' do
      # TODO
    end
  end
end
