# frozen_string_literal: true

module Aggregates
  # Gem identity information.
  module Identity
    NAME = 'aggregates'
    LABEL = 'Aggregates'
    VERSION = '0.3.0'
    VERSION_LABEL = "#{LABEL} #{VERSION}"
  end
end
